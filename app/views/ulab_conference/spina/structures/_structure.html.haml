- return render 'shared/empty_list', name: local_assigns[:name] unless structure.structure_items.any?

%ul{ class: local_assigns[:list_class] }
  - structure.structure_items.sorted_by_structure.each do |structure_item|
    = render structure_item, **local_assigns do |item|
      = yield item
