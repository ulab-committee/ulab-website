.subnav
  = content_for :subnav_buttons
  %nav.subnav-links{ aria: { label: '' } }
    .UnderlineNav-body
      - collection.each do |item|
        %a{ title: item.send(method), class: 'subnav-item', target: '_self', data: { target: 'itemnav.item', action: 'itemnav#select' } }= item.send(method)
