- if has_content? :alert
  .alert
    = content(:alert)
- if has_content? :gallery
  .gallery
    - content(:gallery)&.images&.each do |image|
      = image_tag main_app.url_for(image.variant(resize: '800x533'))
= content(:text).try(:html_safe)
